---
title: "Spring Security Production Guide"
description: "Master production Spring Security 6 architecture, method-level authorization with SpEL, CORS configuration, and RFC 9457 error handling."
---

# Spring Security Production Guide

Spring Security protects backend applications at the outer perimeter before any controller logic executes.

In production systems, security is not just checking whether a user has logged in. A robust backend architecture enforces authentication, role-based authorization, granular resource ownership, strict CORS policies, and RFC 9457 error standardization.

---

## 1. Choose Security Rules by Credential Transport

**CSRF** means a malicious site causes a browser to send an unwanted authenticated request. Browsers attach cookies automatically. Calling an application stateless does not remove this risk.

This example serves two kinds of endpoints:

| Routes | Credentials | CSRF rule |
| :--- | :--- | :--- |
| `/api/auth/**` | Login request or refresh cookie | Keep CSRF protection. Fetch a token before POST requests. |
| Other API routes | Access token in the `Authorization` header only | CSRF can be disabled if these routes never accept cookies, HTTP Basic, or session authentication. |

Add `spring-boot-starter-oauth2-resource-server`. Configure its JWT decoder using [issuer, audience, and algorithm validation](/production/security-jwt#4-2-spring-boot-3-resource-server-configuration). This uses Spring Security's bearer filter instead of an undefined custom JWT filter.

### Two Filter Chains

```java
package com.example.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.core.annotation.Order;
import org.springframework.http.HttpMethod;
import org.springframework.security.config.annotation.method.configuration.EnableMethodSecurity;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configurers.AbstractHttpConfigurer;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.oauth2.server.resource.authentication.JwtAuthenticationConverter;
import org.springframework.security.oauth2.server.resource.authentication.JwtGrantedAuthoritiesConverter;
import org.springframework.security.web.AuthenticationEntryPoint;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.access.AccessDeniedHandler;
import org.springframework.web.cors.CorsConfigurationSource;

@Configuration
@EnableMethodSecurity
public class SecurityConfig {

    @Bean
    @Order(1)
    SecurityFilterChain browserAuth(HttpSecurity http,
            CorsConfigurationSource cors,
            AccessDeniedHandler denied) throws Exception {
        return http
            .securityMatcher("/api/auth/**")
            .cors(c -> c.configurationSource(cors))
            // Default CSRF storage uses the session. This chain may create one.
            .csrf(csrf -> {})
            .sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.IF_REQUIRED))
            .exceptionHandling(e -> e.accessDeniedHandler(denied))
            .authorizeHttpRequests(a -> a
                .requestMatchers(HttpMethod.GET, "/api/auth/csrf").permitAll()
                .requestMatchers(HttpMethod.POST, "/api/auth/login",
                    "/api/auth/register", "/api/auth/refresh").permitAll()
                .anyRequest().denyAll())
            .build();
    }

    @Bean
    @Order(2)
    SecurityFilterChain bearerApi(HttpSecurity http,
            CorsConfigurationSource cors,
            AuthenticationEntryPoint unauthenticated,
            AccessDeniedHandler denied) throws Exception {
        JwtGrantedAuthoritiesConverter roles = new JwtGrantedAuthoritiesConverter();
        // Contract: trusted issuer sends roles: ["USER", "ADMIN"].
        roles.setAuthoritiesClaimName("roles");
        roles.setAuthorityPrefix("ROLE_");
        JwtAuthenticationConverter converter = new JwtAuthenticationConverter();
        converter.setJwtGrantedAuthoritiesConverter(roles);

        return http
            .cors(c -> c.configurationSource(cors))
            .csrf(AbstractHttpConfigurer::disable)
            .sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(a -> a
                .requestMatchers(HttpMethod.GET, "/api/products/**", "/actuator/health").permitAll()
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                .anyRequest().authenticated())
            .exceptionHandling(e -> e.authenticationEntryPoint(unauthenticated)
                .accessDeniedHandler(denied))
            .oauth2ResourceServer(o -> o
                .jwt(j -> j.jwtAuthenticationConverter(converter))
                .authenticationEntryPoint(unauthenticated)
                .accessDeniedHandler(denied))
            .build();
    }
}
```

Spring chooses the first matching chain. The auth chain's `permitAll()` allows access without an access token; it does **not** bypass its CSRF check. The roles converter above replaces default scope mapping; add a combined converter if the API also requires scopes.

### Security Chain Line-By-Line

- `@Configuration` makes this class contribute beans to the Spring application context.
- `@EnableMethodSecurity` activates annotations such as `@PreAuthorize` on service methods.
- `@Bean @Order(1)` registers the browser-auth filter chain first.
- `securityMatcher("/api/auth/**")` limits that chain to auth routes.
- `.csrf(csrf -> {})` keeps CSRF enabled for cookie-carrying browser flows.
- `SessionCreationPolicy.IF_REQUIRED` allows a session when CSRF token storage needs it.
- `permitAll()` means authentication is not required for login/register/refresh routes. It does not mean "skip all filters."
- `@Bean @Order(2)` registers the general bearer-token API chain after the auth chain.
- `.csrf(AbstractHttpConfigurer::disable)` is safe only if these routes never authenticate with cookies, sessions, or HTTP Basic.
- `SessionCreationPolicy.STATELESS` prevents Spring Security from creating server-side login sessions for bearer API calls.
- `.requestMatchers("/api/admin/**").hasRole("ADMIN")` enforces coarse route-level authorization.
- `.anyRequest().authenticated()` requires a valid identity for everything else.
- `.oauth2ResourceServer(...jwt...)` uses Spring's resource-server filter to validate bearer JWTs before controllers run.

Proof tests:

```console
POST /api/auth/login without CSRF token -> 403.
GET /actuator/health without token -> 200.
GET /api/admin/users with USER role -> 403.
GET /api/notes with no token -> 401.
GET /api/notes with valid token -> reaches controller/service authorization.
```

### Give the Browser a CSRF Token

```java
package com.example.security;

import org.springframework.http.CacheControl;
import org.springframework.http.ResponseEntity;
import org.springframework.security.web.csrf.CsrfToken;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class CsrfController {
    public record CsrfResponse(String headerName, String token) {}

    @GetMapping("/api/auth/csrf")
    ResponseEntity<CsrfResponse> csrf(CsrfToken csrf) {
        return ResponseEntity.ok().cacheControl(CacheControl.noStore())
            .body(new CsrfResponse(csrf.getHeaderName(), csrf.getToken()));
    }
}
```

The browser first fetches this endpoint with `credentials: "include"`. It sends the returned token in the returned header name, usually `X-CSRF-TOKEN`, on login and refresh POST requests. It also sends the associated session cookie. Fetch a fresh CSRF token after authentication or logout clears the old one. Refresh responses must use `Cache-Control: no-store`. Set session and refresh cookies to `Secure` in HTTPS deployments and choose `SameSite` for the actual frontend/API site relationship. [Spring Security CSRF reference](https://docs.spring.io/spring-security/reference/6.5/servlet/exploits/csrf.html)

## 2. Authorize the Stored Resource

Never trust an `authorId` supplied beside a `noteId`. An attacker can send their own author ID together with somebody else's note ID. The database must connect the authenticated identity to the target row.

This example assumes `Note.ownerSubject` stores the validated JWT subject from one configured issuer. For multiple issuers, map `(issuer, subject)` to a local account. The create endpoint must assign ownership from authentication, never the request body.

```java
// NoteRepository.java: one atomic, ownership-scoped database statement.
@Modifying
@Query("delete from Note n where n.id = :id and n.ownerSubject = :subject")
int deleteOwnedNote(@Param("id") Long id, @Param("subject") String subject);
```

```java
// NoteService.java: called through the Spring bean proxy.
@PreAuthorize("isAuthenticated()")
@Transactional
public void deleteNote(Long noteId) {
    String subject = SecurityContextHolder.getContext().getAuthentication().getName();
    if (noteRepository.deleteOwnedNote(noteId, subject) == 0) {
        throw new ResponseStatusException(HttpStatus.NOT_FOUND, "Note not found");
    }
}
```

These are repository and service excerpts, not standalone files. They use Spring Data JPA's `@Modifying`, `@Query`, and `@Param`, plus Spring Security's `@PreAuthorize` and `SecurityContextHolder`. The `404` response treats another user's resource like an absent resource. Administrative deletion should have a separate method with `@PreAuthorize("hasRole('ADMIN')")` and an audit record.

With Spring's JWT resource server, the principal is a `Jwt`, and `authentication.name` normally comes from `sub`; it is not a custom object with an `id` property. [Spring Security JWT authentication](https://docs.spring.io/spring-security/reference/6.5/servlet/oauth2/resource-server/jwt.html)

---

## 3. Production CORS Configuration for Frontend SPAs

Cross-Origin Resource Sharing (CORS) is a browser security mechanism that blocks web applications running on one domain (e.g. `https://app.mydomain.com`) from calling APIs on another domain (e.g. `https://api.mydomain.com`) unless explicit headers are present.

### The Fatal CORS Trap
Setting `allowedOrigins("*")` while setting `allowCredentials(true)` causes modern browsers to immediately reject all requests.

### Production CORS Bean

```java
package com.example.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.cors.CorsConfiguration;
import org.springframework.web.cors.CorsConfigurationSource;
import org.springframework.web.cors.UrlBasedCorsConfigurationSource;

import java.util.List;

@Configuration
public class CorsConfig {

    @Bean
    public CorsConfigurationSource corsConfigurationSource() {
        CorsConfiguration config = new CorsConfiguration();
        
        // Explicit allowed origins (Never use "*" in production!)
        config.setAllowedOrigins(List.of(
            "https://app.example.com",
            "https://admin.example.com"
        ));
        
        config.setAllowedMethods(List.of("GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"));
        config.setAllowedHeaders(List.of("Authorization", "Content-Type", "Idempotency-Key", "X-CSRF-TOKEN"));
        config.setExposedHeaders(List.of("Location", "X-Total-Count"));
        config.setAllowCredentials(true);
        config.setMaxAge(3600L); // Cache preflight response for 1 hour

        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/**", config);
        return source;
    }
}
```

---

## 4. Standardized Security Exceptions with RFC 9457

Because the Spring Security filter chain executes **before** the `DispatcherServlet`, exceptions thrown in filters (e.g. invalid JWT or forbidden role) never reach your `@RestControllerAdvice`.

If not customized, Spring returns default empty bodies or HTML error pages. Configure dedicated handlers to return standardized RFC 9457 `ProblemDetail` JSON.

### Custom 401 Unauthorized Handler

```java
package com.example.security;

import com.fasterxml.jackson.databind.ObjectMapper;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ProblemDetail;
import org.springframework.security.core.AuthenticationException;
import org.springframework.security.web.AuthenticationEntryPoint;
import org.springframework.stereotype.Component;

import java.io.IOException;
import java.net.URI;
import java.time.Instant;

@Component
public class CustomAuthenticationEntryPoint implements AuthenticationEntryPoint {

    private final ObjectMapper objectMapper;

    public CustomAuthenticationEntryPoint(ObjectMapper objectMapper) {
        this.objectMapper = objectMapper;
    }

    @Override
    public void commence(
        HttpServletRequest request,
        HttpServletResponse response,
        AuthenticationException authException
    ) throws IOException {
        ProblemDetail problem = ProblemDetail.forStatusAndDetail(
            HttpStatus.UNAUTHORIZED,
            "Authentication token is missing, invalid, or expired."
        );
        problem.setTitle("Unauthorized");
        problem.setType(URI.create("https://api.example.com/errors/unauthorized"));
        problem.setProperty("timestamp", Instant.now());
        problem.setProperty("path", request.getRequestURI());

        new org.springframework.security.oauth2.server.resource.web.BearerTokenAuthenticationEntryPoint()
            .commence(request, response, authException);
        response.setStatus(HttpStatus.UNAUTHORIZED.value());
        response.setContentType(MediaType.APPLICATION_PROBLEM_JSON_VALUE);
        objectMapper.writeValue(response.getOutputStream(), problem);
    }
}
```

### Custom 403 Forbidden Handler

```java
package com.example.security;

import com.fasterxml.jackson.databind.ObjectMapper;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ProblemDetail;
import org.springframework.security.access.AccessDeniedException;
import org.springframework.security.web.access.AccessDeniedHandler;
import org.springframework.stereotype.Component;

import java.io.IOException;
import java.net.URI;
import java.time.Instant;

@Component
public class CustomAccessDeniedHandler implements AccessDeniedHandler {

    private final ObjectMapper objectMapper;

    public CustomAccessDeniedHandler(ObjectMapper objectMapper) {
        this.objectMapper = objectMapper;
    }

    @Override
    public void handle(
        HttpServletRequest request,
        HttpServletResponse response,
        AccessDeniedException accessDeniedException
    ) throws IOException {
        ProblemDetail problem = ProblemDetail.forStatusAndDetail(
            HttpStatus.FORBIDDEN,
            "You do not have the required role or permissions to access this resource."
        );
        problem.setTitle("Forbidden");
        problem.setType(URI.create("https://api.example.com/errors/forbidden"));
        problem.setProperty("timestamp", Instant.now());
        problem.setProperty("path", request.getRequestURI());

        response.setStatus(HttpStatus.FORBIDDEN.value());
        response.setContentType(MediaType.APPLICATION_PROBLEM_JSON_VALUE);
        objectMapper.writeValue(response.getOutputStream(), problem);
    }
}
```

---

## 5. Automated Security Testing

Start with role checks using a Spring test context that imports the actual configuration and both error handlers. Provide a mock `JwtDecoder` in this slice because these tests inject an already authenticated user. These tests cover authorization; they do not verify token signatures.

```java
@WebMvcTest(AdminController.class)
@Import({SecurityConfig.class, CorsConfig.class,
    CustomAuthenticationEntryPoint.class, CustomAccessDeniedHandler.class})
class AdminControllerSecurityTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private JwtDecoder jwtDecoder;

    @Test
    void shouldRejectAnonymousUserFromAdminApi() throws Exception {
        mockMvc.perform(get("/api/admin/metrics"))
            .andExpect(status().isUnauthorized())
            .andExpect(header().string("Content-Type", MediaType.APPLICATION_PROBLEM_JSON_VALUE));
    }

    @Test
    @WithMockUser(username = "bob", roles = {"USER"})
    void shouldRejectNormalUserFromAdminApiWithForbidden() throws Exception {
        mockMvc.perform(get("/api/admin/metrics"))
            .andExpect(status().isForbidden());
    }

    @Test
    @WithMockUser(username = "alice", roles = {"ADMIN"})
    void shouldAllowAdminUserToAccessAdminApi() throws Exception {
        mockMvc.perform(get("/api/admin/metrics"))
            .andExpect(status().isOk());
    }
}
```

---

### Adversarial Tests You Must Add

| Test | Expected evidence |
| :--- | :--- |
| Bob deletes Alice's note while submitting Bob's author ID | `404`; Alice's row remains. |
| Missing/incorrect CSRF token on refresh POST | `403`; no token is rotated. |
| Valid CSRF token and matching session on refresh POST | Auth service receives the request. |
| JWT signed correctly but for another issuer or audience | `401` using the real decoder, not `jwt()` test helpers. |
| Missing token or expired token | Problem JSON plus `WWW-Authenticate: Bearer`. |
| Timestamp in error response | Serializes successfully using Boot's configured `ObjectMapper`. |

Use `@SpringBootTest` with PostgreSQL for ownership and refresh tests. Injecting `@WithMockUser` or the MockMvc `jwt()` helper bypasses cryptographic verification. Test the real decoder separately with keys created for the test.

---

## 6. Active Recall & Interview Mastery

<AccordionGroup>
  <Accordion title="1. Why do enterprise applications configure multiple SecurityFilterChain beans instead of one?">
    Enterprise applications often serve disparate client types with conflicting security models. Browser-based authentication endpoints (`/api/auth/**`) handle logins, refresh tokens, and cookies, requiring CSRF token defense and stateful session support (`SessionCreationPolicy.IF_REQUIRED`). In contrast, API business routes (`/api/v1/**`) rely on cryptographically signed stateless JWT bearer tokens where CSRF is unnecessary and `SessionCreationPolicy.STATELESS` is required. Using multiple `@Order`-annotated `SecurityFilterChain` beans with `securityMatcher()` allows each domain to enforce its ideal security posture without compromising the other.
  </Accordion>

  <Accordion title="2. What happens if the @Order annotations on SecurityFilterChain beans are inverted?">
    Spring Security iterates through filter chains in ascending order of their `@Order` value until it finds the first chain whose `securityMatcher()` matches the request. If a greedy fallback chain (such as one matching `/**` with `@Order(1)`) runs before a specialized chain (such as `/api/auth/**` with `@Order(2)`), all requests are intercepted by the greedy chain. The specialized chain is never evaluated, causing login and CSRF endpoints to fail or be treated as generic stateless bearer routes.
  </Accordion>

  <Accordion title="3. Why does browser CORS configuration require setAllowCredentials(true) with explicit origins?">
    The W3C CORS specification forbids the use of wildcard origins (`*`) when `allowCredentials` is set to `true`. When credentials (cookies or authorization headers) are sent, browsers mandate that the server explicitly declare the trusted origin (e.g., `https://app.example.com`) in the `Access-Control-Allow-Origin` response header. Attempting to return `*` with credentials causes modern browsers to immediately reject the response.
  </Accordion>

  <Accordion title="4. How does Spring Security convert JWT claims into Spring Security GrantedAuthority objects?">
    By default, Spring Security maps JWT scopes into authorities prefixed with `SCOPE_`. In production microservices where JWTs contain custom role arrays (e.g. `{"roles": ["ADMIN", "USER"]}`), we configure a `JwtGrantedAuthoritiesConverter`:
    `roles.setAuthoritiesClaimName("roles"); roles.setAuthorityPrefix("ROLE_");`.
    We attach this to a `JwtAuthenticationConverter`, ensuring method security annotations like `@PreAuthorize("hasRole('ADMIN')")` match the incoming claims accurately.
  </Accordion>

  <Accordion title="5. Why must AuthenticationEntryPoint and AccessDeniedHandler return RFC 9457 Problem Details?">
    Without custom handlers, Spring Security emits empty response bodies with plain HTTP headers, or HTML error pages from Tomcat. This breaks client-side parsing in SPAs and mobile apps. Implementing `AuthenticationEntryPoint` and `AccessDeniedHandler` with `ProblemDetail` guarantees that 401 and 403 responses adhere to standardized RFC 9457 JSON contracts, supplying structured diagnostic error messages and trace IDs.
  </Accordion>

  <Accordion title="6. What is the Double Submit Cookie pattern for CSRF defense in SPAs?">
    In the Double Submit Cookie pattern, the server sets a cryptographically random CSRF token in an accessible cookie. When the frontend JavaScript sends a mutating HTTP request (`POST`, `PUT`, `DELETE`), it reads the cookie and copies the token into a custom request header (e.g., `X-XSRF-TOKEN`). The backend compares the header value against the cookie value; because malicious third-party sites cannot read cross-origin cookies due to the Same-Origin Policy, they cannot forge the matching header.
  </Accordion>

  <Accordion title="7. Why is @EnableMethodSecurity preferred over legacy @EnableGlobalMethodSecurity?">
    `@EnableMethodSecurity` was introduced in Spring Security 6 to replace the deprecated `@EnableGlobalMethodSecurity`. It uses modern, lightweight method interceptors, enables SpEL pre/post annotations (`@PreAuthorize`, `@PostAuthorize`, `@PreFilter`, `@PostFilter`) by default, integrates with `AuthorizationManager`, and eliminates the complex AOP hierarchy of legacy Spring Security.
  </Accordion>

  <Accordion title="8. How do you prevent Broken Object Level Authorization (BOLA) when retrieving user resources?">
    BOLA occurs when an endpoint verifies that a user is authenticated, but fails to check if they own the requested resource ID. To prevent BOLA:
    1. Scope database queries by both resource ID and owner ID: `repository.findByIdAndOwnerId(id, currentUserId)`.
    2. Enforce SpEL method security: `@PreAuthorize("hasRole('ADMIN') or #userId == authentication.principal.id")`.
    3. Return `404 Not Found` instead of `403 Forbidden` for missing/unowned resources to prevent attackers from enumerating valid resource IDs.
  </Accordion>
</AccordionGroup>

---

## 7. Done Checklist

<Check>
- [ ] Configure multiple `SecurityFilterChain` beans with explicit `@Order` precedence.
- [ ] Bind browser authentication and refresh routes to a stateful chain with CSRF protection.
- [ ] Bind general API routes to a completely stateless chain (`SessionCreationPolicy.STATELESS`).
- [ ] Disable CSRF exclusively on stateless API routes that never accept cookies or ambient credentials.
- [ ] Configure CORS with explicit production origins and avoid wildcard `*` with credentials.
- [ ] Map JWT claims to `ROLE_` authorities using `JwtAuthenticationConverter`.
- [ ] Standardize 401 Unauthorized responses using a custom `AuthenticationEntryPoint` returning `ProblemDetail`.
- [ ] Standardize 403 Forbidden responses using a custom `AccessDeniedHandler` returning `ProblemDetail`.
- [ ] Activate modern method security using `@EnableMethodSecurity`.
- [ ] Prevent BOLA/IDOR by scoping database queries by owner identity or SpEL `@PreAuthorize`.
- [ ] Return 404 Not Found instead of 403 Forbidden to prevent resource ID enumeration attacks.
- [ ] Write MockMvc slice tests verifying that unauthenticated requests receive 401 ProblemDetail.
- [ ] Write MockMvc slice tests verifying that unauthorized roles receive 403 ProblemDetail.
- [ ] Verify that preflight HTTP `OPTIONS` requests succeed without requiring authentication.
- [ ] Test cryptographic JWT validation using real key pairs in `@SpringBootTest` environments.
</Check>
